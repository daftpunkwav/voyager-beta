# docs/modules — 模块卡

每个模块一张卡:§6 模块清单的展开;**各领域完整能力清单在此详细设计与持续演进**
(架构文档 §5/§8 只列初始最小集,不封闭)。

新增能力:改服务 `capabilities.py` 注册表 + 更新对应模块卡,架构文档不动。

- `_template.md`:新模块复制即用;
- 已有卡:agent / gateway / sources / notes / graph / office / code-exec / browser /
  llm / settings / platform。
